sqlc generates db.go and queries.sql.go into this directory. Until your first
`sqlc generate`, the directory is empty and the project does not import it —
auth uses hand-rolled queries in internal/auth/queries.go.
